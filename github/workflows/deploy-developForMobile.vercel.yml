name: deploy to vercel

on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest
    container: pandoc/latex

    steps:
      - uses: actions/checkout@v4

      - name: 1. mustache
        run: apk add ruby && gem install mustache

      - name: 2. output
        run: sh ./build.sh

      - name: 3. 디버그: output 디렉토리 목록 출력
        run: ls -al ./output

      - name: 4. push to mobile-test
        id: push_directory
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.POOMASI_TESTREPO_ACCOUNT_GH_TOKEN }}
          DEBUG: true
        with:
          repository: taetae0707/mobile-test
          source-directory: "output"
          destination-github-username: taetae0707
          destination-repository-name: mobile-test
          commit-message: ${{ github.event.commits[0].message }}
          target-branch: develop
